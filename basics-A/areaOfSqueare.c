#include <stdio.h>
int main(){
    int size , result;
    printf("enter the size of square :");
    scanf("%d", &size  );
    result = size*size ;
    printf("result is : %d \n ", result);
    return 0;
}